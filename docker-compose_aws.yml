version: '3'

services:
  flaskapp:
    image: 937215464284.dkr.ecr.eu-west-3.amazonaws.com/speech_api
    # build: ./
    # command: gunicorn -b 0.0.0.0:8000 flask_server:app
    container_name: flaskapp
    # network_mode: host
    ports:
      - 8000:8000
